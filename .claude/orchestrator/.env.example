# PIV Orchestrator — Environment Variables

# Primary auth: OAuth token from Claude Code subscription (recommended)
# Generate via: claude setup-token
CLAUDE_CODE_OAUTH_TOKEN=

# Fallback auth: API key from Anthropic Console (pay-per-token)
# Generate at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Target project directory (defaults to current working directory)
PIV_PROJECT_DIR=

# Model override (defaults to claude-opus-4-6)
PIV_MODEL=
