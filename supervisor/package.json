{
  "name": "piv-supervisor",
  "version": "0.1.0",
  "description": "PIV Project Supervisor — monitors, diagnoses, and fixes stalled orchestrators",
  "type": "module",
  "bin": {
    "piv": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
